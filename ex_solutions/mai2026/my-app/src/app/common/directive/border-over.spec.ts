import { BorderOver } from './border-over';

describe('BorderOver', () => {
  it('should create an instance', () => {
    const directive = new BorderOver();
    expect(directive).toBeTruthy();
  });
});
